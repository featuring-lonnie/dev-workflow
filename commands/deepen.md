---
description: 계획 심화. 기존 계획/Tech Spec 문서를 선택한 영역에서 병렬 에이전트로 심층 분석 후 보강.
---

# /dev-workflow:deepen - 계획 심화

## 개요

기존 계획 문서(Tech Spec, brainstorm)의 특정 영역을 병렬 에이전트로 심층 분석하여 보강합니다. 기술적 깊이, 엣지 케이스, 리스크, 외부 리서치 등 선택한 영역을 집중 분석합니다.

---

## 실행 단계

### 0. 작업 디렉토리 확인 (선행 조건)

**현재 디렉토리가 올바른 프로젝트인지 확인합니다.**

```bash
# 현재 브랜치에서 티켓 ID 추출
git branch --show-current
# 예: feature/FS-533-http-only-cookie → FS

# config.json에서 해당 프로젝트 키의 localPath 확인
# FS → project-b (~/projects/project-b)
```

### 1. 대상 문서 선택

**자동 감지 순서:**

1. 명시 경로: `/deepen docs/plans/BE-123-plan.md`
2. `docs/plans/`에서 현재 브랜치 티켓 매칭
3. `docs/brainstorms/`에서 현재 브랜치 티켓 매칭

```
Tool: Glob
Pattern: "{projectPath}/docs/plans/*{ticket_id}*"
```

```
Tool: Glob
Pattern: "{projectPath}/docs/brainstorms/*{ticket_id}*"
```

**여러 문서 발견 시:**
```
Tool: AskUserQuestion
Question: "어떤 문서를 심화하시겠습니까?"
Options:
  - "{doc_1_name}" - {doc_1_path}
  - "{doc_2_name}" - {doc_2_path}
  - "{doc_3_name}" - {doc_3_path}
```

### 2. 문서 내용 읽기

```
Tool: Read
file_path: "{selected_doc_path}"
```

### 3. 심화 영역 선택

```
Tool: AskUserQuestion
Question: "어떤 영역을 심화하시겠습니까? (복수 선택 가능)"
Options:
  - "기술적 깊이" - API 설계, 데이터 모델, 알고리즘 상세화
  - "엣지 케이스" - 경계값, 동시성, 장애 시나리오 분석
  - "리스크 분석" - 기술적 리스크, 의존성 리스크, 일정 리스크
  - "외부 리서치" - 프레임워크 문서, 업계 모범 사례 조사
multiSelect: true
```

### 4. 병렬 에이전트 분석

**선택한 영역에 대해 병렬 에이전트를 실행합니다:**

#### A. 기술적 깊이 (선택 시)

```
Tool: Task
subagent_type: "oh-my-claudecode:architect-medium"
model: sonnet
prompt: |
  다음 계획 문서의 기술적 설계를 심화 분석해주세요.

  문서 내용:
  {document_content}

  프로젝트 경로: {projectPath}

  분석해야 할 것:
  1. API 설계의 완전성 (누락된 엔드포인트, 파라미터)
  2. 데이터 모델의 정규화/비정규화 적절성
  3. 알고리즘/로직의 정확성
  4. 인터페이스 계약 (입출력 스키마)
  5. 에러 응답 설계
```

#### B. 엣지 케이스 (선택 시)

```
Tool: Task
subagent_type: "oh-my-claudecode:architect-medium"
model: sonnet
prompt: |
  다음 계획 문서의 엣지 케이스를 분석해주세요.

  문서 내용:
  {document_content}

  분석해야 할 것:
  1. 경계값 (빈 입력, 최대값, null)
  2. 동시성 시나리오 (경쟁 상태, 데드락)
  3. 장애 시나리오 (네트워크 실패, DB 타임아웃)
  4. 데이터 정합성 (부분 실패, 트랜잭션)
  5. 사용자 시나리오 (비정상 사용 패턴)

  각 엣지 케이스에 대해 처리 방안을 제안해주세요.
```

#### C. 리스크 분석 (선택 시)

```
Tool: Task
subagent_type: "oh-my-claudecode:architect-medium"
model: sonnet
prompt: |
  다음 계획 문서의 리스크를 분석해주세요.

  문서 내용:
  {document_content}

  분석해야 할 것:
  1. 기술적 리스크 (난이도, 불확실성)
  2. 의존성 리스크 (외부 API, 라이브러리)
  3. 성능 리스크 (병목, 확장성)
  4. 보안 리스크 (취약점, 데이터 노출)
  5. 일정 리스크 (예상 소요 시간, 변수)

  각 리스크에 대해 확률, 영향도, 완화 전략을 제시해주세요.
```

#### D. 외부 리서치 (선택 시)

```
Tool: Task (2개 병렬)
```

**D-1. 프레임워크 문서:**
에이전트 정의: [framework-docs-researcher](../agents/framework-docs-researcher.md) 참조

```
subagent_type: "oh-my-claudecode:researcher"
model: sonnet
prompt: |
  다음 계획에서 사용하는 프레임워크/라이브러리의 공식 문서를 조사해주세요.

  문서 내용: {document_content}
  프레임워크: {detected_framework}

  조사해야 할 것:
  1. 관련 API의 정확한 사양
  2. 공식 권장 패턴
  3. 주의사항 및 제약
```

**D-2. 모범 사례:**
에이전트 정의: [best-practices-researcher](../agents/best-practices-researcher.md) 참조

```
subagent_type: "oh-my-claudecode:researcher"
model: haiku
prompt: |
  다음 주제의 업계 모범 사례를 조사해주세요.

  주제: {topic_from_document}
  맥락: {project_context}

  조사해야 할 것:
  1. 업계 표준 접근법
  2. 보안 가이드라인
  3. 안티 패턴
```

### 5. Plan 모드에서 논의

```
Tool: EnterPlanMode
```

에이전트 분석 결과를 정리하여 사용자와 논의합니다:

- 각 영역의 분석 결과 요약
- 문서에 추가/수정할 내용 제안
- 사용자 의견 수렴

### 6. 문서 업데이트

사용자 승인 후 대상 문서를 업데이트합니다:

- 기존 섹션 보강 (내용 추가)
- 신규 섹션 추가 (엣지 케이스, 리스크 등)
- 참조 링크 추가

**업데이트 표시:**
```markdown
> **심화 분석 ({today})**: {deepen_area} 영역 보강됨
```

### 7. 완료 요약

```
계획 심화 완료

대상: {document_path}
심화 영역:
  - {area_1}: {summary}
  - {area_2}: {summary}

추가된 내용:
  - {addition_1}
  - {addition_2}

다음 단계:
  1. /dev-workflow:work — 보강된 계획으로 작업 시작
  2. /dev-workflow:deepen — 다른 영역 추가 심화 (선택)
```

---

## 사용 예시

```
/dev-workflow:deepen
# 현재 브랜치 티켓의 계획 문서 심화

/dev-workflow:deepen docs/plans/BE-123-plan.md
# 특정 문서 심화

/dev-workflow:deepen --area edge-cases
# 엣지 케이스만 직접 지정하여 심화
```

---

## 예상치 못한 에러 처리

위 에러 외의 예상치 못한 에러가 발생한 경우:

1. **에러 분류 확인**: [공통 에러 핸들링 가이드](../shared/error-handling.md) 참조
2. **인증 에러가 아닌 경우**: GitHub 이슈 등록 여부 확인

```
Tool: AskUserQuestion
Question: "예상치 못한 에러가 발생했습니다. GitHub 이슈로 등록할까요?"
Options:
  - "이슈 등록" - dev-workflow 레포에 버그 리포트 생성
  - "건너뛰기" - 이슈 등록하지 않고 진행
  - "에러 상세 보기" - 에러 정보 확인 후 결정
```

**이슈 등록 시:**
```bash
gh issue create \
  --repo featuring-lonnie/dev-workflow \
  --title "[Bug] deepen: {에러 요약}" \
  --body "{이슈 본문 - shared/error-handling.md 템플릿 참조}" \
  --label "bug"
```

---

## Attribution

문서 업데이트에 config의 attribution을 추가합니다:

- `~/.claude/workflow/config.json`의 `attribution.text` 값을 사용합니다 (하드코딩 금지)
- `attribution.enabled`가 `false`인 경우 생략합니다

```
# config.json 예시
"attribution": {
  "text": "🤖 Written with Claude Code"
}
```
